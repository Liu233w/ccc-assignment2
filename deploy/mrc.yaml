- hosts: localhost
  vars_files:
    - host_vars.yaml
  gather_facts: true

  roles:
  - role: common
  # - role: list-images # only used to test of openstack works
  - role: volume
  - role: security-group
  - role: instances
  # - role: volume-snapshot # Seems like that snapshot is not available